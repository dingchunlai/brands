<%= include_jquery_easy_validator %>
<%= top_bar :title => '地区管理页',
            :button => ['地区列表', admin_districts_path(:city_id => params[:city_id])],
            :breadcrumbs => [
                    ['Homepage', coupon_admin_index_path],
                    ['Districts', admin_districts_path(:city_id => params[:city_id])]
            ]
%>
<%=flash[:notice] && "<div style='padding: 15px; color:blue; font-size: 22px'>#{flash[:notice]}</div>"%>
<% form_for @district, :url => admin_districts_path(:city_id => params[:city_id]) do |f| %>
    <div style="color:green"><%= flash[:notice] %></div>
    <%= f.error_messages %>
    <div class="table">
      <img src="/stylesheets/pretty_admin/img/bg-th-left.gif" width="8" height="7" alt="" class="left"/>
      <img src="/stylesheets/pretty_admin/img/bg-th-right.gif" width="7" height="7" alt="" class="right"/>
      <table class="listing form" cellpadding="0" cellspacing="0">
        <tr>
          <th class="full" colspan="2">创建地区</th>
        </tr>
        <tr>
          <td class="first" width="172"><strong><%= f.label :city_id %></strong></td>
          <td class="last"><%= f.select :city_id, City.find(:all).collect { |city| [city.name, city.id] }, :selected=> params[:city_id].to_i, :include_blank => "请选择", :class => "text", :reg => '^\S+$', :tip => "必须*" %></td>
        </tr>

        <tr class="bg">
          <td class="first" width="172"><strong><%= f.label :name %></strong></td>
          <td class="last"><%= f.text_field :name, :class => "text", :reg => '^\S+$', :tip => "必须*" %></td>
        </tr>
        <tr class="bg">
          <td class="first" width="172"><strong><%= f.label :code_name %></strong></td>
          <td class="last"><%= f.text_field :code_name, :class => "text", :reg => '^\S+$', :tip => "必须*" %></td>
        </tr>
        <tr>
          <td colspan="2"><%= f.submit '提交' %></td>
        </tr>
      </table>
    </div>
<% end %>
